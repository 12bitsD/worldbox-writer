name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-quality:
    uses: ./.github/workflows/reusable-backend-quality.yml

  frontend-quality:
    uses: ./.github/workflows/reusable-frontend-quality.yml
